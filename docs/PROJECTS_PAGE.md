# Страница проектов

## Обзор

Реализована полноценная страница проектов с системой табов для каждого проекта. Каждая вкладка отображает только данные, относящиеся к конкретному проекту, в удобном двухколоночном макете.

## Местоположение

- **Файл**: `/dashboard/projects.html`
- **URL**: `/dashboard/projects.html`
- **Навигация**: Меню → Проекты (📁)

## Функциональность

### 1. Система табов
- **Вкладки проектов**: Каждый проект пользователя получает свою вкладку
- **Цветовая индикация**: Цвет проекта отображается на вкладке
- **Счетчики**: Количество элементов в каждом проекте
- **Пустое состояние**: Специальная вкладка для пользователей без проектов

### 2. Двухколоночный макет
**Левая колонка:**
- Задачи проекта, сгруппированные по времени:
  - ⚠️ Просроченные
  - 🔥 На сегодня
  - ⏰ На завтра
  - 📅 Позже
  - 📋 Без даты

**Правая колонка:**
- 📅 Встречи проекта
- 🛒 Покупки проекта
- 📄 Последние 5 статей (в разработке)

### 3. Интерактивные элементы
- **Чекбоксы**: Отметка выполнения задач и покупок
- **Модальные окна**: Создание новых проектов и задач
- **Кнопки действий**: Быстрое добавление элементов

## Компоненты

### Компонент табов
**Местоположение**: UI Kit → "📑 Компонент табов"

**Элементы**:
- `.tabs-container` - контейнер табов
- `.tabs-header` - заголовки табов
- `.tab-item` - элемент таба
- `.tab-color` - цветовой индикатор
- `.tab-title` - название таба
- `.tab-count` - счетчик элементов
- `.tabs-content` - содержимое табов
- `.tab-content` - содержимое отдельного таба

### Элементы проекта
**Секции**:
- `.project-content` - основной контейнер
- `.project-column` - колонка макета
- `.project-section` - секция данных

**Элементы**:
- `.task-item` - элемент задачи
- `.task-group` - группа задач
- `.event-item` - элемент встречи
- `.shopping-item` - элемент покупки

## Адаптивность

### Мобильные устройства (≤768px)
- **Табы**: Горизонтальная прокрутка
- **Макет**: Одноколоночный
- **Элементы**: Компактные размеры
- **Кнопки**: Постоянное отображение

### Десктоп (>768px)
- **Табы**: Горизонтальное размещение
- **Макет**: Двухколоночный
- **Элементы**: Полный размер
- **Кнопки**: Появление при наведении

## API интеграция

### Используемые endpoints
- `GET /api/user-projects` - получение проектов пользователя
- `GET /api/tasks` - получение задач
- `GET /api/events` - получение событий
- `GET /api/shopping-by-lists` - получение покупок
- `POST /api/projects` - создание проекта
- `POST /api/tasks` - создание задачи
- `POST /api/tasks/{id}/toggle` - переключение задачи
- `POST /api/shopping/{id}/toggle` - переключение покупки

### Поток данных
1. Загрузка проектов пользователя
2. Создание табов для каждого проекта
3. Загрузка данных для активного проекта
4. Отображение данных в соответствующих секциях
5. Обновление счетчиков элементов

## Модальные окна

### Создание проекта
**Поля**:
- Название проекта (обязательное)
- Цвет проекта (color picker)

**Функции**:
- Валидация данных
- Создание через API
- Обновление списка проектов

### Создание задачи
**Поля**:
- Название задачи (обязательное)
- Описание задачи
- Срок выполнения
- Приоритет (низкий/средний/высокий)

**Функции**:
- Привязка к активному проекту
- Создание через API
- Обновление списка задач

## Группировка задач

### Алгоритм группировки
```javascript
const now = new Date();
const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
const tomorrow = new Date(today.getTime() + 24 * 60 * 60 * 1000);

// Логика определения группы
if (!task.due_date) -> "Без даты"
if (dueDate < today) -> "Просроченные"
if (dueDate === today) -> "На сегодня"
if (dueDate === tomorrow) -> "На завтра"
else -> "Позже"
```

### Визуальная индикация
- **Просроченные**: Красная левая граница
- **На сегодня**: Оранжевая левая граница
- **На завтра**: Зеленая левая граница
- **Приоритет**: Цветные левые границы элементов

## Пустые состояния

### Нет проектов
- Специальная вкладка "Нет проектов"
- Кнопка создания первого проекта
- Информативное сообщение

### Нет данных в проекте
- Информативные сообщения в каждой секции
- Кнопки для добавления элементов
- Объяснение функций секций

## Производительность

### Оптимизации
- Ленивая загрузка данных проектов
- Кеширование загруженных данных
- Загрузка только активного проекта
- Анимации только для видимых элементов

### Индикаторы загрузки
- Спиннеры во время загрузки
- Анимированные переходы
- Feedback для пользователя

## Стили

### Цветовая схема
- **Основные**: Наследование от темы
- **Акценты**: Цвета проектов
- **Состояния**: Успех, ошибка, предупреждение
- **Группы**: Цветовая индикация по времени

### Анимации
- **Переходы**: 0.3s ease-in-out
- **Появление**: fadeIn анимация
- **Ховер**: Плавные изменения фона
- **Спиннер**: Бесконечная ротация

## Доступность

### Клавиатурная навигация
- Tab для перехода между элементами
- Enter для активации кнопок
- Escape для закрытия модальных окон

### Семантика
- Правильные HTML теги
- ARIA атрибуты
- Логическая структура заголовков

## Тестирование

### Автоматические тесты
- Доступность страницы
- Корректность компонентов
- Интеграция с API
- Адаптивность

### Функциональные тесты
- Создание проектов
- Переключение табов
- Группировка задач
- Интерактивность элементов

## Интеграция с навигацией

### Добавлено в меню
- Иконка: 📁
- Название: "Проекты"
- Позиция: После "Встречи"

### Заголовок страницы
- Автоматическое определение активной страницы
- Обновление заголовка в navbar

## Файлы

### Основные файлы
- `/dashboard/projects.html` - основная страница
- `/dashboard/ui-components.css` - стили компонентов
- `/dashboard/navigation.js` - интеграция в навигацию

### Документация
- `/docs/PROJECTS_PAGE.md` - данная документация
- `/test_projects_page.py` - тесты страницы

## Будущие улучшения

### Планируемые возможности
- Система статей для проектов
- Drag & drop для элементов
- Фильтрация и поиск
- Экспорт данных проекта
- Шаблоны проектов

### Оптимизации
- Виртуализация больших списков
- Offline режим
- Real-time обновления
- Кеширование изображений

## Примеры использования

### Создание нового проекта
1. Нажать "Создать проект"
2. Ввести название и выбрать цвет
3. Нажать "Создать"
4. Проект появится в новой вкладке

### Работа с задачами
1. Перейти на вкладку проекта
2. Нажать "Добавить" в секции задач
3. Заполнить форму задачи
4. Задача появится в соответствующей группе

### Отметка выполнения
1. Нажать на чекбокс рядом с элементом
2. Элемент изменит состояние
3. Обновится счетчик в табе

## Статус

✅ **Полностью готово к использованию**
- Все основные функции реализованы
- Адаптивный дизайн работает
- Интеграция с API завершена
- Тесты пройдены
- Документация создана